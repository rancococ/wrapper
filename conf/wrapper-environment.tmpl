{
    "JmxExporter": {
        "Enabled": "${WRAPPERS_JMX_EXPORTER_ENABLED}",
        "Port": "${WRAPPERS_JMX_EXPORTER_PORT}"
    },
    "HeapDumpEnabled": "${WRAPPERS_HEAP_DUMP_ENABLED}",
    "PrintGcEnabled": "${WRAPPERS_PRINT_GC_ENABLED}",
    "Xms": "${WRAPPERS_XMS}",
    "Xmx": "${WRAPPERS_XMX}",
    "Xss": "${WRAPPERS_XSS}",
    "MetaspaceSize": "${WRAPPERS_METASPACE_SIZE}",
    "MaxMetaspaceSize": "${WRAPPERS_MAX_METASPACE_SIZE}",
    "RemoteDebug": {
        "Enabled": "${WRAPPERS_REMOTE_DEBUG_ENABLED}",
        "Suspend": "${WRAPPERS_REMOTE_DEBUG_SUSPEND}",
        "Port": "${WRAPPERS_REMOTE_DEBUG_PORT}"
    },
    "JmxRemote": {
        "Enabled": "${WRAPPERS_JMX_REMOTE_ENABLED}",
        "Ssl": "${WRAPPERS_JMX_REMOTE_SSL}",
        "Auth": "${WRAPPERS_JMX_REMOTE_AUTH}",
        "RmiServerHostname": "${WRAPPERS_JMX_REMOTE_RMI_SERVER_HOSTNAME}",
        "RmiRegistryPort": "${WRAPPERS_JMX_REMOTE_RMI_REGISTRY_PORT}",
        "RmiServerPort": "${WRAPPERS_JMX_REMOTE_RMI_SERVER_PORT}"
    },
    "HttpListenPort": "${WRAPPERS_HTTP_LISTEN_PORT}",
    "ShutdownPort": "${WRAPPERS_SHUTDOWN_PORT}",
    "OtherParameters": [
        ${WRAPPERS_OTHER_PARAMETERS}
    ]
}